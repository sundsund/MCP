apiVersion: v1
kind: ConfigMap
metadata:
  name: dispute-resolution-config
data:
  KAFKA_BOOTSTRAP_SERVERS: "msk-cluster-endpoint:9092"
  KAFKA_TOPIC: "dispute-resolution"
  DATABASE_URL: "postgresql://user@aurora-endpoint:5432/dispute_db"
  KAFKA_SECURITY_PROTOCOL: "SASL_SSL"
  KAFKA_SASL_MECHANISMS: "SCRAM-SHA-512"
